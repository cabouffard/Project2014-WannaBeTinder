.msg-wrap
  .media.msg
    .pull-left
      = small_user_avatar(message.user)
    .media-body
      %small.pull-right.time
        = icon("fa fa-clock-o")
        = message.created_at.strftime('%H:%M')
      %h5.media-heading= message.user.name
      %small.col-lg-10= message.body
