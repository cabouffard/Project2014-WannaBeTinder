<%= render "shared/header", title: t("devise.passwords.new.title"), intro: nil %>

<%= simple_form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :post } do |f| %>
  <%= f.error_notification %>
  <%= f.input :email, placeholder: true, label: false, error: false, autofocus: true %>
  <div>
    <%= f.button :button, t("devise.passwords.new.submit"), class: "btn-primary btn-block" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
