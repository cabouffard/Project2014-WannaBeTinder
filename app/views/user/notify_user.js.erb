$(function() {
  publisher = faye.publish('/notifications/<%= @user.id %>', {
    message: '<%= t("notification.private_notification", user: current_user.name,
                                                        locale: @user.locale) %>'
  });
});


